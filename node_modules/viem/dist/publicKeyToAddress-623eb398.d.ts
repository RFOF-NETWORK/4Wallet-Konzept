import { Address } from 'abitype';
import { A as Account } from './typedData-46b4d81f.js';
import { H as Hex } from './chain-5507c6f0.js';

declare function parseAccount(account: Address | Account): Account;

/**
 * @description Converts an ECDSA public key to an address.
 *
 * @param publicKey The public key to convert.
 *
 * @returns The address.
 */
declare function publicKeyToAddress(publicKey: Hex): Address;

export { publicKeyToAddress as a, parseAccount as p };
